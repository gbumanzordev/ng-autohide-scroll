import { AutoHideScrollDirective } from './auto-hide-scroll.directive';

describe('AutoHideScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoHideScrollDirective();
    expect(directive).toBeTruthy();
  });
});
